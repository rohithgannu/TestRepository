trigger Sampletrigger on Account (after insert) {
    
   
  
     /*   List<Account> accList=new List<Account>();
        for(Account acc:trigger.new){
            accList.add(acc);
            
        }
        if(trigger.isAfter){
            if(trigger.isInsert) {
                if(accList.Size()>0){
                Samplehandler.accountlistMethod(accList);
                }

           }
        }*/
    }